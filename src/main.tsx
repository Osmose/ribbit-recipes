import React from 'react';
import ReactDOM from 'react-dom/client';
import App from 'ribbit/components/App';
import 'ribbit/styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
